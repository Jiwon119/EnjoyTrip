<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.member.model.mapper.MemberMapper">

	<insert id="insertMember"></insert>

	<select id="selectMember" parameterType="String" resultType="MemberDto">
		select * from members
		where id=#{id}
	</select>

	<update id="updateMember"></update>

	<update id="modifyMember"></update>

	<delete id="deleteMember" parameterType="String">
		delete from members
		where id=#{id}
	</delete>

	<select id="searchPass"></select>

	<select id="login"></select>

</mapper>